# 模板：安装时复制到 /etc/systemd/system/ 并按实际部署环境修改
# User 与 ExecStart 路径（本机运行的以 /etc/systemd/system/ 下的副本为准）。
[Unit]
Description=LiteGate 数据库在线备份（管理 API 触发 VACUUM INTO）
After=litegate.service

[Service]
Type=oneshot
User=app
ExecStart=/usr/bin/bash /opt/litegate/deploy/backup.sh
